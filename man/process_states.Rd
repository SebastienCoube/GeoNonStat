% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeoNonStat.R
\name{process_states}
\alias{process_states}
\title{Title}
\usage{
process_states(
  hm,
  covariates,
  observed_field,
  vecchia_approx,
  init_tk = -4,
  seed = 1
)
}
\arguments{
\item{hm}{a hierarchical model (obtained with `process_hierarchical_model()`)}

\item{covariates}{The list of covariates obtained with `process_covariates`}

\item{observed_field}{TODO}

\item{vecchia_approx}{TODO}

\item{init_tk}{numerical value to initialize transition kernels}

\item{seed}{numeric value, seed used for reproducibility purposes. Default to 1}
}
\value{
a list
}
\description{
Title
}
\examples{
set.seed(100)
nobs = 10000
observed_locs = cbind(runif(nobs), runif(nobs))
observed_field = rnorm(nobs)

X = as.data.frame(cbind(runif(nobs), rnorm(nobs), rpois(nobs, 5)))
vecchia_approx = createVecchia(observed_locs, 12, ncores=1)
PP = createPP(vecchia_approx)

covariates = list(
  X = process_covariates(X = X, vecchia_approx), 
  noise_X = process_covariates(X = X, vecchia_approx, PP),
  range_X = process_covariates(X = X, vecchia_approx)
)
hm = process_hierarchical_model(vecchia_approx = vecchia_approx,
  noise_PP = PP, noise_log_scale_bounds = NULL,
  range_PP = PP, range_log_scale_bounds = NULL,
  observed_locs,
  matern_smoothness = 1.5,
  observed_field = observed_field,
  covariates = covariates,
  anisotropic = TRUE) 

process_states(
    hm,
    covariates,
    observed_field,
    vecchia_approx,
    init_tk = -4
)
}
\keyword{internal}
