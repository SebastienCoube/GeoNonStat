% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Scores.R
\name{createSplitData}
\alias{createSplitData}
\title{Split data between train and test}
\usage{
createSplitData(locs, data, n_clust = NULL, prop_test = 0.1)
}
\arguments{
\item{locs}{a matrix of spatial locations.}

\item{data}{a list containing vectors or data.frames to split into
train/test}

\item{n_clust}{the number of clusters. If NULL, will be set to 1\% of the data.}

\item{prop_test}{the proportion of clusters used as test locations.
Default to 0.01.}
}
\description{
Split a GeoNonStat object between test and train samples
}
\examples{
n <- 10000
observed_locs <- round(cbind(runif(n), runif(n)), 3)
X <- as.data.frame(matrix(rnorm(n * 4), ncol = 4))
range_X <- as.data.frame(matrix(rnorm(n * 4), ncol = 4))
noise_X <- as.data.frame(matrix(rnorm(n * 4), ncol = 4))
y <- rnorm(n)
datalist <- list("X" = X, "range_X" = range_X, "noise_X" = noise_X, "y" = y)
traintest <- createSplitData(observed_locs, datalist, prop_test = c(0.1, 0.01))
}
