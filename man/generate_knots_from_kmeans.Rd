% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PP.R
\name{generate_knots_from_kmeans}
\alias{generate_knots_from_kmeans}
\title{Generate spatial knots using k-means clustering}
\usage{
generate_knots_from_kmeans(knots_number, locs)
}
\arguments{
\item{knots_number}{Integer. The number of knots (i.e., clusters) to generate.}

\item{locs}{A matrix of spatial coordinates (typically with two columns for 2D locations).}
}
\value{
A matrix of size \code{knots_number} x ncol(\code{locs}) containing the spatial knot coordinates (cluster centers).
}
\description{
Selects a set of spatial knots using k-means clustering from the observed locations.
If the number of locations is large, a subsample of up to 10,000 points is used,
and a small random perturbation is added to break ties and improve cluster separation.
}
\examples{
locs <- cbind(runif(5000), runif(5000))
knots <- generate_knots_from_kmeans(100, locs)
plot(locs, col = "grey", pch = 16, cex = 0.5)
points(knots, col = "red", pch = 19)
}
