% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulateFakeData.R
\name{createGnsSimulatorParameters}
\alias{createGnsSimulatorParameters}
\title{From a GeoNonStatSimulator object, creates parameters at the right format}
\usage{
createGnsSimulatorParameters(
  gns_simulator,
  range_intercept = NULL,
  range_PP_log_var = NULL,
  noise_intercept = NULL,
  noise_PP_log_var = NULL,
  field_log_var = NULL,
  verbose = TRUE
)
}
\arguments{
\item{gns_simulator}{an object created by `createGnsSimulator()`}

\item{range_intercept}{numeric value, for the intercept of range. Default to NULL (set to 0)}

\item{range_PP_log_var}{log var for PP range. Default to NULL (0). Numeric value of 
length `1 + gns_simulator$anisotropic`.}

\item{noise_intercept}{numeric value, for the intercept of noise. Default to NULL (set to 0)}

\item{noise_PP_log_var}{numeric value, for the log variance of noise PP. Default to NULL (set to -6)}

\item{field_log_var}{numeric value, log variance of the field. Default to NULL (set to 0)}

\item{verbose}{logical, default to TRUE. Active verbose mode ?}
}
\value{
a list
}
\description{
From a GeoNonStatSimulator object, creates parameters at the right format
}
\examples{
set.seed(2)
nlocs <- 15000
observed_locs <- cbind(runif(nlocs), runif(nlocs))
vecchia_approx <- createVecchia(observed_locs, m = 6)

# fixed effects
X <- data.frame(
  V1 = observed_locs[, 1],
  V2 = rnorm(nrow(observed_locs)),
  V3 = rnorm(nrow(observed_locs)),
  V4 = rnorm(nrow(observed_locs))
)

# X_range = as.data.frame(observed_locs)

PP_range <- createPP(vecchia_approx, knots = 16, matern_range = .3)
PP_noise <- createPP(vecchia_approx, knots = 500, matern_range = .06)


# full monty
gns_simulator <- createGnsSimulator(
  vecchia_approx = vecchia_approx,
  X = X,
  noise_X = X, noise_PP = PP_noise,
  range_X = NULL, range_PP = PP_range,
  anisotropic = TRUE
)
gns_simulator_parameters <- createGnsSimulatorParameters(gns_simulator)
}
