% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualisation.R
\name{plotPointillistPainting}
\alias{plotPointillistPainting}
\title{Plots a spatial variable like a \href{https://en.wikipedia.org/wiki/Pointillism}{pointillist painting}, the color intensity being the variable value.}
\usage{
plotPointillistPainting(
  locs,
  field,
  override_layout = T,
  add = FALSE,
  alpha = TRUE,
  legend = TRUE,
  ...
)
}
\arguments{
\item{locs}{numeric matrix of spatial locations}

\item{field}{numerical vector, interest variable to define color of points}

\item{add}{logical, default to FALSE. Add on existing plot ?}

\item{alpha}{logical, default to TRUE. Add transparency to colors ?}

\item{legend}{logical, default to TRUE. Add legend ?}

\item{...}{additional graphical parameters, sent to `plot` or,
if `add=TRUE`, to `points`}
}
\value{
a plot component (a `plot` if `add == FALSE`, `points` if `add ==TRUE`)
}
\description{
Plots a spatial variable like a \href{https://en.wikipedia.org/wiki/Pointillism}{pointillist painting}, the color intensity being the variable value.
}
\examples{
locs <- matrix(rnorm(2000), ncol = 2)
plotPointillistPainting(locs = locs, field = rnorm(1000))
}
