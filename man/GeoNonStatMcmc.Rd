% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MCMC.R
\name{GeoNonStatMcmc}
\alias{GeoNonStatMcmc}
\title{Run the MCMC on a `GeoNonStat` object, with parallel execution on chains}
\usage{
GeoNonStatMcmc(
  object,
  n_chains_in_parallel = NULL,
  n_threads_per_chain = 5,
  n_iterations = 100,
  seed = 1
)
}
\arguments{
\item{object}{an object of class `GeoNonStat`}

\item{n_chains_in_parallel}{numeric, number of chains in parallel, default to NULL}

\item{n_threads_per_chain}{numeric, number of threads by markov chain, default to 5}

\item{n_iterations}{numeric value, number of iterations of MCMC. Default to 100}

\item{seed}{integer value, seed used for reproducibility purposes. Default to 1}
}
\value{
a GeoNonStat object, with updated state (last state by chain)
and records (params records for all iterations, by chain)
}
\description{
Run the MCMC on a `GeoNonStat` object, with parallel execution on chains
}
\details{
The MCMC is run using [future::plan()].

\preformatted{
 if (n_chains_in_parallel == 1) {
   # For no parallelisation
   future::plan(future::sequential)
 } else {
   # For multisession or multicore (prefered):
   if (parallelly::supportsMulticore()) {
     future::plan(future::multicore, workers = n_chains_in_parallel)
   } else {
     future::plan(future::multisession, workers = n_chains_in_parallel)
   }
 }
}
You can see `?future::plan` to define your plan more precisely.
}
\examples{
processedGns <- GeoNonStatMcmc(
  gnsDemo,
  n_chains_in_parallel = 1,
  n_threads_per_chain = 1, # example in sequential
  n_iterations = 15
)
}
